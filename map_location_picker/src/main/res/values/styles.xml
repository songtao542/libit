<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/action_menu_text_color</item>
        <item name="android:actionMenuTextColor">@color/action_menu_text_color</item>
        <item name="actionMenuTextColor">@color/action_menu_text_color</item>
    </style>

    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="navigationIcon">@drawable/ic_cancel</item>
        <item name="android:theme">@style/PopupOverlay</item>
        <item name="popupTheme">@style/PopupOverlay</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@color/lp_toolbar_bg_color</item>
        <item name="titleTextAppearance">@style/ToolbarTextAppearance</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="contentInsetLeft">0dp</item>
        <item name="contentInsetStartWithNavigation">0dp</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentPadding">0dp</item>
        <item name="titleMargin">0dp</item>
        <item name="titleTextColor">@android:color/transparent</item>
    </style>

    <style name="ToolbarTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
