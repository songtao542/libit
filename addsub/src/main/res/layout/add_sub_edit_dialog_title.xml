<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/topPanel"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/title_template"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingStart="@dimen/add_sub_dialog_title_padding"
        android:paddingTop="@dimen/add_sub_dialog_title_padding"
        android:paddingEnd="@dimen/add_sub_dialog_title_padding"
        android:paddingBottom="@dimen/add_sub_dialog_title_padding">

        <ImageView
            android:id="@android:id/icon"
            android:layout_width="32dip"
            android:layout_height="32dip"
            android:layout_marginEnd="8dip"
            android:scaleType="fitCenter"
            android:src="@null" />

        <!--style="?android:attr/windowTitleStyle"-->
        <TextView
            android:id="@+id/alertTitle"
            style="?android:attr/windowTitleStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:singleLine="true"
            android:text="@string/add_sub_dialog_title"
            android:textSize="@dimen/add_sub_dialog_title_text_size" />
    </LinearLayout>

</LinearLayout>
